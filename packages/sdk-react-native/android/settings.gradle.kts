// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: 2026 ScriptX
//
// Minimal settings — the sample app (Plan 06-06) consumes this library via
// `includeBuild("../../../packages/sdk-android/android")` from its own settings file, so this
// library does NOT itself include any sibling modules.

rootProject.name = "sdk-react-native"
