{
  "version": 1,
  "description": "Cross-language redaction regex patterns. Loaded by codegen for iOS/Android SDKs; mirrors the canonical set in packages/sdk-core/src/redaction/engine.ts. Update both in lockstep.",
  "patterns": [
    {
      "id": "jwt",
      "regex": "[A-Za-z0-9_-]{8,}\\.[A-Za-z0-9_-]{8,}\\.[A-Za-z0-9_-]{8,}",
      "replacement": "[REDACTED:JWT]",
      "description": "JWT (RFC 7519) three-segment base64url; matches the inline shape used by sdk-core engine.ts JWT_INLINE"
    },
    {
      "id": "bearer",
      "regex": "(?i)Bearer\\s+[A-Za-z0-9._~+/-]+=*",
      "replacement": "[REDACTED]",
      "description": "RFC 6750 Bearer scheme; redacted at network layer in addition to header allowlist"
    },
    {
      "id": "luhn-cc",
      "regex": "\\b\\d[\\d\\s-]{11,21}\\d\\b",
      "replacement": "[REDACTED:CC]",
      "description": "Credit card range (13-19 digits with optional spaces/dashes); Luhn validation applied separately by language-native code"
    },
    {
      "id": "ssn-us",
      "regex": "\\b\\d{3}-\\d{2}-\\d{4}\\b",
      "replacement": "[REDACTED:SSN]",
      "description": "US Social Security Number XXX-XX-XXXX format"
    }
  ]
}
