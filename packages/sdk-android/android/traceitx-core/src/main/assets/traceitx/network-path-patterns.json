{
  "version": 1,
  "description": "Cross-language URL/path sensitivity patterns. Each language SDK loads at codegen-time; values matching `regex` are redacted by `redact` strategy ('value' = redact the matched value only, 'whole' = redact the entire match).",
  "sensitive_paths": [
    {
      "id": "auth-tokens-in-query",
      "regex": "[?&](token|access_token|id_token|api_key|sig)=[^&]+",
      "redact": "value",
      "replacement": "[REDACTED]"
    }
  ]
}
