{
  "name": "@traceitx/sdk-android",
  "private": true,
  "version": "1.2.0",
  "description": "TraceItX native Android SDK — Gradle multi-module build (traceitx-core, traceitx-reporter-ui, traceitx-protocol, traceitx-gradle-plugin). Published to Maven under com.traceitx; not consumed via pnpm.",
  "license": "MIT",
  "scripts": {
    "build": "cd android && ./gradlew assembleRelease",
    "test": "cd android && ./gradlew test",
    "clean": "cd android && ./gradlew clean",
    "publish:maven-local": "cd android && ./gradlew publishAllToMavenLocal"
  }
}
