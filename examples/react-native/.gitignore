# SPDX-License-Identifier: MIT
# SPDX-FileCopyrightText: 2026 ScriptX
# Copyright 2026 ScriptX
#
# Expo managed workflow — native dirs are generated by `expo prebuild` and
# must NOT be committed. The authoritative native config lives in `app.json`
# (expo config) + `plugins/with-traceitx-workspace.js` (workspace composite-
# build wiring).

# Native build outputs from `expo prebuild`
/ios
/android

# Expo internals
.expo/
.expo-shared/
web-build/
dist/
.hermes-error-test/

# Node / build / OS noise
node_modules/
*.log
.DS_Store

# Synced from repo-root .env by scripts/sync-env.mjs — do not edit by hand.
.env
.env.local
